export default function Alphanumeric(
    valid : boolean,
    value : string
) : string {

    if(valid) {

        return `string is alphanumeric`;

    } else {

        return `string is not alphanumeric`;
    }
}
